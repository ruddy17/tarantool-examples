# Для удобства, открываем три консоли side-by-side, по одной для каждого инстанса
# Запускаем master1:
cd master1
tarantool -i master1.lua
# Видим, что мастер запустился, попробовал приконнектиться к другому мастеру, не смог и пообещал пробовать снова, пока не получится.
# Инсертим в интерактивной консоли немного записей:
box.space.test:insert({nil, replica, 12345, 'testdata'})
box.space.test:insert({nil, replica, 23456, 'moredata'})
# Запускаем master2:
cd master2
tarantool -i master2.lua
# Видим, что участники репликасета увидели друг друга и мастер 2 получил изменения из мастера 1. Проверим, заселектив все записи:
box.space.test:select()
# Видим, что данные из первого инстанса засинхронизировались. 

# Можем проверить и в другую сторону. Во втором мастере сделаем:
box.space.test:insert({nil, replica, 34567, 'testdata2'})
box.space.test:insert({nil, replica, 45678, 'moredata2'})
# И в первом проверим, что записи перенеслись
box.space.test:select()

# Можем подключить к репликасету еще одну реплику, слэйва:
